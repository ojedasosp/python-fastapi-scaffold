services:
  db:
    image: postgres:16
    container_name: postgres
    ports: 
      - 5432:5432
    environment:
      - POSTGRES_USER=santiago
      - POSTGRES_PASSWORD=development
      - POSTGRES_DB=mod8
    volumes:
      - /home/santiago/Documents/Projects/modulo8/docker/postgres:/var/lib/postgresql/data

